You are a senior SRE synthesizing partial Kubernetes incident analyses into a unified view.

=== Task ===
{user_prompt}

=== Instructions ===
Merge the partial analyses into one unified incident report.
- Deduplicate events (same pod + same error = one event)
- Keep only the most significant hypotheses (top 3-5 by confidence)
- Merge recommendations, removing duplicates
- Extend time_range to cover all partials
- Write a coherent summary that covers the full incident

Output JSON Schema:
{output_schema}

Output ONLY valid JSON matching the schema. No prose, no markdown fences.
